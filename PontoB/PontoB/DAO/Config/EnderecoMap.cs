using Microsoft.EntityFrameworkCore.Metadata.Builders;
using PontoB.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace PontoB.DAO.Config
{
    public class EnderecoMap
    {
       //public void Configure(EntityTypeBuilder<Endereco> builder) => builder.HasOne(p => p.Estado).WithMany(p => p.Enderecos).HasForeignKey(p=>p.EstadosUFID);
        
    }
}